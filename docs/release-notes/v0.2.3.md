# katana-markdown-model v0.2.3

## 変更内容

- `just update` を実行し、直接依存 `thiserror` を 2.0.21 へ更新。
- Cargo.lock の互換範囲内の推移的依存を更新。
- 公開 API、renderer-neutral DTO、library-only の責務境界は変更なし。

## 検証

- `just release-check`（format、Clippy、AST lint、全 workspace テスト、OpenSpec、package、publish dry-run）
- GitHub Actions の macOS / Ubuntu / Windows CI と release-preflight
