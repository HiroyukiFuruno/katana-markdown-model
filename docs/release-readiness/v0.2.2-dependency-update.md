# v0.2.2 依存関係更新

## 対象と完了条件

Rust の直接・推移的依存、lockfile、GitHub Actions の action を最新互換版へ更新する。公開 API と library-only 境界を維持し、検証基準を下げない。

- [x] 最新 master から release/v0.2.2 を作成
- [x] just update と cargo outdated --workspace で Rust 依存を更新・確認
- [x] GitHub Actions の最新版確認・更新
- [x] just release-check（just check を含む）
- [ ] Draft PR とレビュー、指摘対応、Ready、required checks
- [ ] master へ merge、GitHub Release と crates.io 公開確認
- [ ] branch/worktree 整理

## 依存関係

serde 1.0.229、serde_json 1.0.151、thiserror 2.0.20 へ更新。sha2 0.11.0、katana-ast-lint 0.5.1、tempfile 3.27.0 は最新。manual-harness の依存と Cargo.lock も更新。

## 公開状態

未公開。

## Actions とローカル検証

actions/checkout v7.0.1 は最新のため維持。dtolnay/rust-toolchain は最新 master、Swatinem/rust-cache は v2.9.2、taiki-e/install-action は just tag の最新 SHA に固定し、3 workflow で統一した。

just release-check は成功（workspace tests 23件、AST lint、Clippy、package、publish dry-run）。active OpenSpec change はなし。公開 API の仕様変更はない。

追跡 Issue: https://github.com/HiroyukiFuruno/katana-markdown-model/issues/14

## レビュー対応

- [/] P1: 全3 workflow の SHA 固定した install-action に tool: just を明示。固定先の default も just だが、設定だけで意図が読めるようにした。
