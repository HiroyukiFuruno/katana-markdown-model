# v0.1.0 Structure Inspection確認結果

## 結論

PASS。

2026-05-07に、KMMの構造確認harnessとfixture test方針を確認した。

KMMのrelease判定は目視GUIではなくfixture testを正本にする。`just harness-up` は補助的な構造確認入口である。

## 確認コマンド

```bash
cd /Users/hiroyuki_furuno/works/private/katana-markdown-model
just harness-check
just harness-up /Users/hiroyuki_furuno/works/private/katana/assets/fixtures/sample.md
cargo package --locked --allow-dirty --list
```

## 確認項目

- [x] KatanA `sample.md` のMarkdown入力が表示される
- [x] KMM node一覧が表示される
- [x] 選択nodeのsource range、line-column、raw snippet、fingerprintが表示される
- [x] metadata解決状態に `Resolved`、`Moved`、`Unresolved`、`Conflict` が表示される
- [x] package一覧に `tools/manual-harness` が含まれない

## 確認結果

- `just harness-check`: PASS
- `just harness-up /Users/hiroyuki_furuno/works/private/katana/assets/fixtures/sample.md`: PASS
- `cargo package --locked --allow-dirty --list`: PASS
- `tools/manual-harness` の公開crate混入: なし
