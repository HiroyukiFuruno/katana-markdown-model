# v0.1.0 Release PR Readiness

## 結論

2026-05-07時点で、`release/v0.1.0` から `master` へのrelease PRはmerge済みである。

実公開、公開後verify、branch hygieneも完了している。

## PR

- PR: https://github.com/HiroyukiFuruno/katana-markdown-model/pull/1
- base: `master`
- head: `release/v0.1.0`
- 状態: MERGED
- merge commit: `17a9a61e9e7bcd194c9e8f8764dfbc496e80d8aa`

## Branch Protection

`master` は次のcheckを必須にしている。

- `Test and Build (macos-latest)`
- `Test and Build (ubuntu-latest)`
- `Test and Build (windows-latest)`
- `preflight`

`required_status_checks.strict` は有効である。

## PR Checks

PR #1 の必須checkは成功している。

- `Test and Build (macos-latest)`: SUCCESS
- `Test and Build (ubuntu-latest)`: SUCCESS
- `Test and Build (windows-latest)`: SUCCESS
- `preflight`: SUCCESS

## v0.1.0に含めないもの

parser精度、metadata照合精度、対応Markdown構文の追加は `v0.1.0` のrelease条件に含めない。

これらは公開境界を動かす可能性があるため、`v0.1.1` 以降の小さなOpenSpec changeで扱う。
