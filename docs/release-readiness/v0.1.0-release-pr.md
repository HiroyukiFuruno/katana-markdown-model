# v0.1.0 Release PR Readiness

## Summary

The `v0.1.0` release PR path completed, and the release was later published from `master`.

## PR

- PR: https://github.com/HiroyukiFuruno/katana-markdown-model/pull/1
- base: `master`
- head: `release/v0.1.0`
- state: MERGED
- merge commit: `17a9a61e9e7bcd194c9e8f8764dfbc496e80d8aa`

## Branch Protection

`master` requires these checks:

- `Test and Build (macos-latest)`
- `Test and Build (ubuntu-latest)`
- `Test and Build (windows-latest)`
- `preflight`

`required_status_checks.strict` is enabled.

## PR Checks

The required checks for PR #1 passed:

- `Test and Build (macos-latest)`: SUCCESS
- `Test and Build (ubuntu-latest)`: SUCCESS
- `Test and Build (windows-latest)`: SUCCESS
- `preflight`: SUCCESS

## Excluded From v0.1.0

Parsing precision, metadata matching precision, and additional Markdown syntax coverage were not release conditions for `v0.1.0`.

Those items can change the public boundary, so they belong in smaller `v0.1.1` or later OpenSpec changes.
