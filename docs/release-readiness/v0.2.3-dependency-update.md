# v0.2.3 依存関係更新

## 対象と完了条件

`just update` により Rust の直接・推移的依存と lockfile を最新互換版へ更新する。公開 API と library-only 境界を維持し、既存の検証基準を下げない。

- [x] 最新 master から `release/v0.2.3` を作成
- [x] `just update` で Rust 依存を更新
- [x] `just release-check`（`just check` を含む）
- [ ] Draft PR とレビュー、指摘対応、Ready、required checks
- [ ] master へ merge、GitHub Release と crates.io 公開確認
- [ ] branch/worktree 整理

## 依存関係

直接依存 `thiserror` を 2.0.20 から 2.0.21 へ更新した。Cargo.lock の互換範囲内の推移的依存も更新した。公開 API の仕様変更はない。

## 公開状態

未公開。
